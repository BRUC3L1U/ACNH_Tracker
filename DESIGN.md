# 界面设计约定

## 视觉方向
岛屿自然观察手册。暖白纸面和深苔绿导航承载真实图鉴图片，条目紧凑，优先呈现名称、收集状态和捕捉条件。

## 色彩
背景 `#f5f3eb`，条目纸面 `#fffef9`，侧栏 `#e8ecdf`，正文 `#283e34`，次要文字 `#536153`。主色 `#35634f` 表示选择和收集，陶土色 `#a95531` 表示当前时间和焦点，错误色 `#a34530`。

## 字体
正文使用本地系统字体与 PingFang SC / Microsoft YaHei，无网络字体。页面标题使用同一系统无衬线字体，32px，700；条目名 16px，650；操作文字 12–14px。正文默认 500 字重，小标签 600，时间 600。中文不加负字距，数量使用等宽数字。

## 组件
分类以实色选中背景表示。筛选使用 6px 圆角按钮，展开区沿用原筛选状态；默认折叠。收集使用圆形勾选框，已收集行保留图片与文字对比度。出现月份使用深绿底与浅色数字，未出现月份使用浅灰底，当前月份另加陶土色描边。鉴伪在条目内展开。

## 布局
桌面侧栏 216px，内容左右间距 48px。进度两列，列表以横向分隔线组织，不使用独立阴影卡片。生物图占两行，名称和捕捉条件排列在其右侧。

## 层次
通过纸面、侧栏和底色区别层级。阴影仅用于提示和确认弹窗；进度条为纯色，无宽度动画。

## 约束
保留图鉴 ID、备份格式和收集语义。不加入介绍大段文案、无功能装饰按钮或新的网络字体。筛选、收集、导航键盘操作无动画；尊重 reduced motion。所有主要按钮至少 40px 高。

## 响应式
760px 以下侧栏变为顶部分类栏，内容边距 20px，生物图片 56px，月份与时间移至下一行。360px 以下边距 12px。验证 1280px、375px、320px，以及图片失败、筛选展开和鉴伪展开状态。

## 后续修改参考
沿用 index.html 的原生 CSS 变量。新增次要按钮：透明背景，文字 `#536153`，边框 `#dedfd3`，6px 圆角，至少 40px 高。新增图鉴条目：纸面 `#fffef9`，分隔线 `#dedfd3`，正文 `#283e34`，16px / 650 名称，12px 条件文字。不得改变已有收集和备份行为来适配布局。
